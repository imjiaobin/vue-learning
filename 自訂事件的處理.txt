#自訂事件處理

    ##組件的使用+自訂事件
        -如果我的組件,要使用主要的App.vue的變數,就要這樣做
        -在要觸發的主檔案的標籤設定屬性@自訂事件 = "自訂事件"
        -在要實際運作的子檔案的標籤內設定@自訂事件 = "$emit('myevent')"
        -通常在A組件呼叫一個響應式方法,也想同時連動到其他組件,就需要透過自訂事件的方式去達到效果
        -A子組件透過事件觸發並呼叫了父主件,可以再透過B子組件的設定的屬性去同時達到A組件的效果
