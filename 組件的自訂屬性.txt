#自訂屬性

    -如何自訂屬性?在組件的script區,defineProps(["組件屬性"]);定義要接收的屬性名稱(可以設定多個屬性)
    -在需要的地方以模板仔入組件屬性 <nav>{{組件屬性}}</nav>
    -到最上層的App.vue的該組建標籤中的屬性帶入
    <template>
        <組件名稱 自訂屬性名稱="資料"></nav>
    </template>
    -在不同的地方都可以使用,每次都可以自己決定屬性怎麼設定